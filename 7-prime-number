public Boolean isPrime(Integer num) {
    //code here
    if(num==2||num==3){
        return true;
    }
    
    for(Integer i=2;i<Math.sqrt(num);i++){
        if(Math.mod(n,i)==0){
            return false;
        }
    }
    return true;
}