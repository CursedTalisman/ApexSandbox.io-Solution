public void setParent(Account acc, Contact con, Opportunity opp) {
    //code here
    if(acc.Id!=NULL || acc!=NULL){
        con.AccountId = acc.Id;
        opp.AccountId = acc.Id;
    }
    //&&con.AccountId==NULL&&opp.AccountId==NULL
}