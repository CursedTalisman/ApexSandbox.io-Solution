public Boolean sameParent(Account acc, Contact con, Opportunity opp) {
    //code here
    if(con.AccountId==acc.Id && opp.AccountId==acc.Id && (con.AccountId!=NULL || opp.AccountId!=NULL)){
        return true;
    }
    return false;
}