public Boolean sameParent(Contact c, Opportunity opp) {
    //code here

    if(c.AccountId==opp.AccountId && (c.AccountId!=NULL || opp.AccountId!=NULL)){
        return true;
    }
    return false;
}